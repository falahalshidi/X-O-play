# 🚀 رفع المشروع على Netlify

## الخطوات:

### 1. رفع الملفات على GitHub
```bash
git init
git add .
git commit -m "Initial commit - Static version for Netlify"
git branch -M main
git remote add origin YOUR_GITHUB_REPO_URL
git push -u origin main
```

### 2. ربط المشروع مع Netlify

1. اذهب إلى [Netlify](https://www.netlify.com/)
2. اضغط على **"Add new site"** → **"Import an existing project"**
3. اختر **GitHub** واختر المستودع الخاص بك
4. في إعدادات البناء (Build settings):
   - **Build command**: اتركه فارغاً أو `echo "No build needed"`
   - **Publish directory**: `.` (نقطة)
5. اضغط **Deploy site**

### 3. الملفات المطلوبة

المشروع الآن يحتوي على:
- ✅ `index.html` - الصفحة الرئيسية
- ✅ `static/` - مجلد CSS و JavaScript
- ✅ `netlify.toml` - إعدادات Netlify
- ✅ `static/js/gameLogic.js` - منطق اللعبة (JavaScript)
- ✅ `static/js/game.js` - منطق التحكم (بدون Flask)

### 4. ملاحظات مهمة

- المشروع الآن **static** بالكامل (لا يحتاج Python/Flask)
- كل المنطق يعمل على JavaScript في المتصفح
- الذكاء الاصطناعي يعمل محلياً في المتصفح

### 5. اختبار الموقع

بعد الرفع، ستحصل على رابط مثل:
`https://your-site-name.netlify.app`

افتح الرابط وتأكد من أن اللعبة تعمل بشكل صحيح!

---

## 🔧 استكشاف الأخطاء

إذا واجهت مشاكل:

1. **الصفحة لا تعمل**: تأكد من أن `index.html` في المجلد الرئيسي
2. **الـ CSS لا يظهر**: تأكد من أن المسار `static/css/style.css` صحيح
3. **JavaScript لا يعمل**: افتح Developer Console (F12) وتحقق من الأخطاء

---

**تم! المشروع جاهز للرفع على Netlify 🎉**

