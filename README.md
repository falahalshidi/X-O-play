# 🎮 X-O Game | لعبة إكس أو

<div dir="rtl">

## 📖 نظرة عامة

لعبة إكس أو (Tic-Tac-Toe) تفاعلية مع واجهة عصرية وذكاء اصطناعي لا يُقهر! اللعبة مصممة بنظام بطولة من 3 جولات مع تتبع النقاط.

### ✨ المميزات

- 🎯 **نمطان للعب**:
  - لاعب ضد لاعب (Player vs Player)
  - لاعب ضد الذكاء الاصطناعي (Player vs AI)

- 🏆 **نظام البطولة**: 3 جولات مع تجميع النقاط
- 🤖 **ذكاء اصطناعي متقدم**: باستخدام خوارزمية Minimax
- 🎨 **تصميم عصري**: ألوان متدرجة جميلة مع تأثيرات Glassmorphism
- ✨ **أنيميشنز سلسة**: تحريكات وتأثيرات بصرية جذابة
- 📱 **تصميم متجاوب**: يعمل على جميع الأجهزة
- 🌐 **دعم اللغة العربية**: واجهة كاملة بالعربية مع دعم RTL

</div>

---

## 🚀 Quick Start

### Prerequisites

- Python 3.8 or higher
- Modern web browser (Chrome, Firefox, Edge, Safari)

### Installation

1. **Clone or download the project**
```bash
cd play
```

2. **Install Python dependencies**
```bash
pip install -r requirements.txt
```

3. **Run the server**
```bash
python app.py
```

4. **Open your browser**
```
http://localhost:5000
```

That's it! The game is now running 🎉

---

<div dir="rtl">

## 📂 هيكل المشروع

```
play/
│
├── app.py                  # تطبيق Flask الرئيسي (Backend)
├── game_logic.py           # منطق اللعبة وخوارزمية Minimax
├── requirements.txt        # متطلبات Python
│
└── static/
    ├── index.html          # الصفحة الرئيسية
    ├── css/
    │   └── style.css       # التصميم والأنيميشنز
    └── js/
        └── game.js         # منطق التحكم في الواجهة
```

---

## 🎓 شرح الكود - للأغراض التعليمية

### 1️⃣ Backend (Python + Flask)

#### `game_logic.py` - منطق اللعبة الأساسي

**الوظائف الرئيسية:**

##### `check_winner(board)`
```python
def check_winner(board):
    """
    فحص الفائز في اللعبة
    Returns: 'X', 'O', أو None
    """
```
- **الشرح**: تفحص جميع احتمالات الفوز (3 صفوف + 3 أعمدة + 2 أقطار)
- **القيمة المرجعة**: رمز الفائز أو None إذا لم يفز أحد

##### `minimax(board, depth, is_maximizing, ai_symbol, player_symbol)`
```python
def minimax(board, depth, is_maximizing, ai_symbol, player_symbol):
    """
    خوارزمية Minimax للذكاء الاصطناعي
    Returns: النقاط (10 للفوز، -10 للخسارة، 0 للتعادل)
    """
```
- **الشرح**: خوارزمية تجعل الذكاء الاصطناعي لا يُقهر!
  - تستكشف جميع الحركات الممكنة
  - تختار الحركة التي تعظم فرص الفوز
  - تستخدم العمق (depth) لتفضيل الفوز السريع

**مثال على عمل الخوارزمية:**
```
إذا كانت اللوحة:
X | O | X
---------
O | X | _
---------
_ | _ | O

الذكاء الاصطناعي (O) يفحص:
1. وضع O في الخانة 5 ← اللاعب يمكنه الفوز في الخانة 6
2. وضع O في الخانة 6 ← يمنع اللاعب من الفوز ويفوز هو!
3. وضع O في الخانة 7 ← اللاعب يمكنه الفوز

النتيجة: يختار الخانة 6 (أفضل حركة)
```

#### `app.py` - خادم Flask

**API Endpoints:**

| Endpoint | Method | الوصف |
|----------|--------|-------|
| `/` | GET | عرض الصفحة الرئيسية |
| `/api/init_game` | POST | تهيئة لعبة جديدة |
| `/api/make_move` | POST | تنفيذ حركة اللاعب |
| `/api/ai_move` | POST | حساب وتنفيذ حركة الذكاء الاصطناعي |
| `/api/next_round` | POST | الانتقال للجولة التالية |
| `/api/reset_game` | POST | إعادة تعيين اللعبة |

**مثال على استخدام API:**

```javascript
// تنفيذ حركة اللاعب
fetch('/api/make_move', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ position: 4 }) // الضغط على الخانة الوسطى
})
.then(response => response.json())
.then(data => {
    console.log('النتيجة:', data);
    // { success: true, board: [...], winner: null, ... }
});
```

---

### 2️⃣ Frontend (HTML + CSS + JavaScript)

#### `style.css` - التصميم العصري

**تقنيات التصميم المستخدمة:**

1. **CSS Variables** - متغيرات لونية قابلة لإعادة الاستخدام
```css
:root {
    --primary-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    --shadow-glow: 0 0 20px rgba(102, 126, 234, 0.5);
}
```

2. **Glassmorphism** - تأثير الزجاج المطفي
```css
background: rgba(255, 255, 255, 0.1);
backdrop-filter: blur(10px);
border: 2px solid rgba(255, 255, 255, 0.1);
```

3. **Animations** - تحريكات سلسة
```css
@keyframes pulse {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.05); }
}
```

4. **Responsive Design** - تصميم متجاوب
```css
@media (max-width: 768px) {
    /* تعديلات للشاشات الصغيرة */
}
```

#### `game.js` - منطق الواجهة

**الوظائف الرئيسية:**

##### التنقل بين الشاشات
```javascript
function showScreen(screenName) {
    // إخفاء جميع الشاشات وإظهار الشاشة المطلوبة
    Object.values(screens).forEach(screen => 
        screen.classList.remove('active')
    );
    screens[screenName].classList.add('active');
}
```

##### معالجة حركات اللاعبين
```javascript
async function makeMove(position) {
    // 1. إرسال الحركة للـ Backend
    const response = await fetch('/api/make_move', {...});
    
    // 2. استقبال النتيجة
    const data = await response.json();
    
    // 3. تحديث الواجهة
    updateBoardDisplay();
    
    // 4. فحص الفائز
    if (data.winner) {
        handleWin(data.winner);
    }
}
```

---

## 🎮 كيفية اللعب

### الخطوات:

1. **أدخل اسمك** في شاشة الترحيب
2. **اختر نمط اللعب**:
   - 👥 لاعب ضد لاعب: العب مع صديقك
   - 🤖 لاعب ضد الذكاء الاصطناعي: تحدى الكمبيوتر
3. **العب 3 جولات** واجمع أكبر عدد من النقاط
4. **الفائز** هو من يحصل على أكبر عدد من الانتصارات!

### قواعد اللعبة:

- اللاعب الأول (X) يبدأ دائماً
- ضع رمزك في الخانة الفارغة بالضغط عليها
- الفائز هو من يصل إلى 3 رموز متتالية (صف، عمود، أو قطر)
- إذا امتلأت اللوحة دون فائز = تعادل

---

## 🔧 التعديلات والتخصيص

### تغيير الألوان

في ملف `style.css`، عدّل المتغيرات في `:root`:

```css
:root {
    --primary-gradient: linear-gradient(135deg, #your-color1, #your-color2);
    --x-color: #your-x-color;
    --o-color: #your-o-color;
}
```

### تغيير عدد الجولات

في ملف `app.py`، عدّل الشرط:

```python
if game_state['round'] >= 3:  # غيّر 3 إلى العدد المطلوب
```

وفي ملف `index.html`:

```html
<div class="round-total">من 3</div>  <!-- غيّر 3 -->
```

---

## 🐛 استكشاف الأخطاء

### المشكلة: لا يعمل السيرفر

**الحل:**
```bash
# تأكد من تثبيت Flask
pip install Flask

# شغّل السيرفر مرة أخرى
python app.py
```

### المشكلة: الذكاء الاصطناعي لا يستجيب

**الحل:**
- افتح Developer Console في المتصفح (F12)
- تحقق من وجود أخطاء في console
- تأكد من أن السيرفر يعمل على `localhost:5000`

### المشكلة: التصميم لا يظهر بشكل صحيح

**الحل:**
- احذف الـ cache بالضغط على Ctrl+Shift+R
- تأكد من وجود ملف `style.css` في مجلد `static/css/`

---

## 📚 موارد تعليمية إضافية

### لفهم خوارزمية Minimax:
- [Minimax Algorithm Explained (YouTube)](https://www.youtube.com/watch?v=l-hh51ncgDI)
- [Game Theory - Minimax](https://www.geeksforgeeks.org/minimax-algorithm-in-game-theory-set-1-introduction/)

### لفهم Flask:
- [Flask Documentation](https://flask.palletsprojects.com/)
- [Flask Tutorial (Arabic)](https://www.youtube.com/results?search_query=flask+tutorial+arabic)

### لفهم CSS Animations:
- [CSS Animations Guide](https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_Animations/Using_CSS_animations)
- [Glassmorphism CSS](https://hype4.academy/tools/glassmorphism-generator)

---

## 🤝 المساهمة

هذا المشروع تعليمي ومفتوح للجميع! يمكنك:

- إضافة مميزات جديدة
- تحسين التصميم
- إصلاح الأخطاء
- ترجمة المشروع للغات أخرى

---

## 📄 الرخصة

هذا المشروع مفتوح المصدر ومتاح للاستخدام التعليمي والشخصي.

---

## 👨‍💻 المطور

تم تطوير هذا المشروع كمثال تعليمي لتعلم:
- بناء تطبيقات الويب باستخدام Flask
- برمجة الألعاب
- خوارزميات الذكاء الاصطناعي (Minimax)
- تصميم واجهات المستخدم الحديثة

</div>

---

## 📧 Contact

For questions or suggestions, feel free to reach out!

Happy Gaming! 🎮✨

---

<div align="center">

### Made with ❤️ using Python & Flask

**[⬆ Back to Top](#-xo-game--لعبة-إكس-أو)**

</div>
